# Names and descriptions live in strings.json (the translatable layout);
# this file carries only structure and selectors.
bypass_timed:
  fields:
    serial_number:
      required: true
      selector:
        text:
    duration:
      required: true
      default: 30
      selector:
        number:
          min: 1
          max: 1440
          unit_of_measurement: min

set_clock:
  fields:
    serial_number:
      required: true
      selector:
        text:
